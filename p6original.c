#include<stdio.h>
int string_length(char *a)
{
  int i;
  for(i=0;a[i]!='\0';i++);
  return i;
}
int string_ncmp(char *a1,char *a2)
{
  for(int i=0;i<n && a1[i]==a2[i] && a1[i] !='\0';i++)
    return a1[i] - a2[i];
}
int string_index(char *string,char *substring)
{
  int 11=string_length(s);
  int 12=string_length(substring);
  if 11 < 12
    return -1;
}
int main()
{
  
}